# Add the CV file
git add media/gushiken-cv.pdf

# Commit with a clear message
git commit -m "Fix missing CV PDF file"

# Push to GitHub
git push origin main

Write-Host "CV PDF fix pushed successfully. The CV should now be visible on the site." -ForegroundColor Green 