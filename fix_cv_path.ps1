# Add the updated about.md file
git add about.md

# Commit with a clear message
git commit -m "Update CV path to use the draft version"

# Push to GitHub
git push origin main

Write-Host "CV path has been updated. The CV should now be visible on the site after GitHub Pages rebuilds." -ForegroundColor Green 